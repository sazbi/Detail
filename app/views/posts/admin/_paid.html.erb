<div class="modal fade" id="paidModalCenter" tabindex="-1" role="dialog" aria-labelledby="paidModalTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="paidModalTitle">Update Records</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        How would you like to mark the Following Records?
        <table class="table" id="modal-table" onload="printNames()">
        </table>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <%= f.submit "Submitted", class: "btn btn-primary", name: 'status', value: 'submitted' %>
        <%= f.submit "Approved", class: "btn btn-info", name: 'status', value: 'approved' %>
        <%= f.submit "Paid", class: "btn btn-success", name: 'status', value: 'paid' %>
        <%= f.submit "Rejected", class: "btn btn-danger", name: 'status', value: 'rejected' %>
      </div>
    </div>
  </div>
</div>
