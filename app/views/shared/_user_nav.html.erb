<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#"><%= current_user.stores.first.name.humanize %></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item <%= active?(root_path) %>">
        <%= link_to "Current Week", root_path, class: 'nav-link' %>
      </li>
      <li class="nav-item <%= active?(new_post_path) %>">
        <%= link_to "New Entry", new_post_path, class: 'nav-link' %>
      </li>
    </ul>
    <span class="navbar-text">
      <%= current_payweek[:days_left] %> Days left to submit payroll.
    </span>
    <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: 'btn btn-danger btn-sm'%>
  </div>
</nav>